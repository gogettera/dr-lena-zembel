
import '@testing-library/jest-dom';
import { vi } from 'vitest';

// Make vi available in the global scope for tests
// This ensures that all test files can use vi without importing it
global.vi = vi;

// Set up any mocks or global test configuration here

