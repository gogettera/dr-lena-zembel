
import React from 'react';
import LanguageHome from './LanguageHome';

// Index now directly renders the home page since we only have Hebrew
const Index: React.FC = () => {
  return <LanguageHome />;
};

export default Index;
